<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>
    <%= room.name %>
  </title>

  <% if(room.renderStatic) { %>
  <%- favicon %>
  <% } else { %>
  <link rel="icon" type="ico" href="<%= room.baseURL %>/favicon.ico">
  <script src="https://cdn.jsdelivr.net/npm/quill@2.0.2/dist/quill.js"></script>
  <script src="https://cdn.socket.io/4.8.1/socket.io.min.js"></script>
  <% } %>

  <script>
    const roomId = '<%= room.roomId %>';
    const roomName = '<%= room.name %>'
    const rootNote = '<%= room.rootNote %>';
    const theme = '<%= room.theme || "light" %>';
    const createdOn = '<%= room.createdOn %>';
    const params = new URLSearchParams(window.location.search);
    const editToken = params.get("editToken");
    const canEdit = '<%= room.canEdit %>' === 'true';
    const baseURL = '<%= room.baseURL %>';
    let editMode = false;

    console.log("theme: " + theme);
  </script>
  <% if(room.renderStatic) { %>
  <!-- Statically rendered data 
    - you may edit the data of the notes here, or add custom CSS properties, or any modifications you like!
  -->
  <style>
    <%- css %>
  </style>
  <% } else {%>
  <link rel="stylesheet" href="<%= room.baseURL %>/style.css">
  <% } %>
</head>

<body>
  <% if(room.renderStatic) { %>
  <!-- Feel free to use your own logo here, or remove it entirely -->
  <div class="logo" id="logo1"></div>
  <div class="logo" id="logo2"></div>
  <img src="<%= icons.logo %>" alt="marginalia logo" class="logo-text">
  <% } else { %>
  <a href="<%= room.baseURL %>/">
    <div class="logo" id="logo1"></div>
  </a>
  <a href="<%= room.baseURL %>/">
    <div class="logo" id="logo2"></div>
  </a>
  <img src="<%= room.baseURL %>/logo.svg" class="logo-text" alt="marginalia logo">
  <% } %>

  <% if(room.canEdit) { %>
  <%- include('partials/dialogs.ejs') %>
  <%- include('partials/toolbar.ejs') %>
  <% } %>

  <% if(room.renderStatic) { %>
  <!-- Here are the HTML contents of each annotation. Feel free to edit them! -->
  <% for(const [noteId, noteData] of Object.entries(notes)) { %>
  <div class="static-note" data-id="<%= noteId %>" data-width="<%= noteData.noteOptions.width || 340 %>">
    <%- noteData.noteHtml %>
  </div>

  <% } %>
  <!-- End of note data -->
  <% } %>

  <div id="notes"></div>

  <div id="note-buttons" class="buttons">

    <% if(room.renderStatic) { %>
    <img src="<%= icons.close %>" draggable="false" class="btn" alt="close button" id="close-note">
    <img src="<%= icons.drag %>" draggable="false" class="drag" alt="move note" id="drag-note">
    <img src="<%= icons.unbreak %>" draggable="false" class="btn" alt="restore button" id="restore-note">
    <% } else { %>
    <img src="<%= room.baseURL %>/icons/close.svg" draggable="false" class="btn" alt="close button" id="close-note">
    <img src="<%= room.baseURL %>/icons/28_drag.svg" draggable="false" class="drag" alt="move note" id="drag-note">
    <img src="<%= room.baseURL %>/icons/29_unbreak.svg" draggable="false" class="btn" alt="restore button" id="restore-note">
    <% } %>

    <% if(room.canEdit) { %>
    <img src="<%= room.baseURL %>/icons/05_trash.svg" draggable="false" class="btn" alt="remove note" id="remove-note">
    <img src="<%= room.baseURL %>/icons/26_resize.svg" draggable="false" class="handle" alt="resize note" id="resize-note">
    <% } // if(room.canEdit) %>

  </div>

  <% if(room.renderStatic) { %>
  <script type="module">
    <%- include('partials/generated/roomStatic.ejs') %>
  </script>
  <% } else { %>
  <script src="<%= room.baseURL %>/js/room.js" defer type="module"></script>
  <% } %>

  <% if(room.canEdit) { %>
  <script src="<%= room.baseURL %>/js/noteEdit.js" defer type="module"></script>
  <% } %>
</body>

</html>